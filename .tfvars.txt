# Copy this to terraform.tfvars and fill in your values
# NEVER commit terraform.tfvars to Git!

aws_region = "us-east-1"

# REQUIRED: Your IP address (find it: curl ifconfig.me)
my_ip_address = "YOUR_IP_HERE/32"  # Example: "203.0.113.45/32"

# REQUIRED: Your EC2 key pair name
key_pair_name = "your-key-name"

# Optional: Change if needed
ami_id        = "ami-0c02fb55b35e8eb69"  # Amazon Linux 2023 (us-east-1)
instance_type = "t3.micro"
